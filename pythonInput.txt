import sys
import os
'''a>b且a<c时候，最大值为c
'''
"""a>b且a>c时候，#最大值为a
   a<b且b<c时候，'''最大值为c
   a<b且b<c时候，"最大值为c"
"""

if __name__ == "__main__":
	num1=5;
	num2=6;
	num3=4;
	if  num1 < num2 :#a<b时候，最大值暂定为b
		max = num2;
	elif num1 < num3 :
		max = num3;#'''a>b且a<c时候，最大值为c
	else:
		max = num1;#a<b且b<c时候，"""最大值为c
	if num2 < num3:
		max = num3;#a<b且b<c时候，"最大值为c"
	else:
		max = num2;
	print("\\较大的数是：\*%d*\n",max);